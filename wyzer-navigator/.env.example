# Google Analytics 4 (GA4). Defaults to the production tag (G-QQKWMXNVSH);
# override per environment if needed:
# PUBLIC_GA_ID=G-QQKWMXNVSH
# Log analytics events to the console instead of sending them (local dev):
# PUBLIC_ANALYTICS_DEBUG=true
