/** Mirror of Prisma enums for the web client — keeps types in sync. */
export type CompanySize = 'SOLO' | 'MICRO' | 'SMALL' | 'MEDIUM' | 'LARGE';
export type MemberRole = 'OWNER' | 'ADMIN' | 'MEMBER';
export type Plan = 'FREE' | 'PRO' | 'TEAM';
